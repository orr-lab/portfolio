{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "db:migrate": "node --env-file-if-exists=.env.local db/migrate.ts",
    "db:seed": "node --env-file-if-exists=.env.local db/seed.ts",
    "db:check": "node --env-file-if-exists=.env.local db/check.ts",
    "db:seed:starter": "node --env-file-if-exists=.env.local db/seed-starter.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.8.0",
    "marked": "^18.0.12",
    "next": "^16.3.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^22.20.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2"
  },
  "description": "A portfolio site for work that does not all look alike. Seven layouts, everything editable from a phone.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/orr-lab/portfolio.git"
  }
}
